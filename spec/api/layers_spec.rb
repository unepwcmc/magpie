require "spec_helper"

resource "Layer" do

  # get "/layers" do
# 
    # let(:tenant_layer) { create(:tenant_layer) }
# 
    # example_request "Getting the list of layers" do
      # explanation "curl localhost:3000/layers"
      # do_request
      # status.should == 200
      # response_body.should be_json_eql [layer].to_json
    # end
  # end
  pending("Getting the list of layers")

end
